// Cadena.prototipo.subcadena()
// El substring()método devuelve la parte del stringíndice desde el inicio hasta el índice final y excluyendolo, o hasta el final de la cadena si no se proporciona un índice final.

// Algoritmo de ejemplo:

let cadena = 'Mozilla';

const strSubs =(a) =>{
return a.substring(2, 3);
}

console.log(strSubs(cadena));