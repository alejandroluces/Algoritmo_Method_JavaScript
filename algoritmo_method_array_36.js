// Array.prototype.toString()
// El toString()método de Arrayinstancias devuelve una cadena que representa la matriz especificada y sus elementos.

// Algoritmo de ejemplo:

const array1 = [1, 2, 'a', '1a'];

const array2 = (array) => {
  return array.toString();
}

console.log(array2(array1));