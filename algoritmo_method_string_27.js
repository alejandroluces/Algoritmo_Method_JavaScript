// String.prototype.startsWith()
// El startsWith()método determina si una cadena comienza con los caracteres de una cadena especificada, regresando trueo falsesegún corresponda.

// Algorirmo de busqueda:

let str="Hola mundo";

const strSearch =(a) =>{

    return str.startsWith(a);
}
console.log(strSearch("Hola"));

    

