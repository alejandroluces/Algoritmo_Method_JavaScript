// Cadena.prototipo.toLocaleUpperCase()
// El toLocaleUpperCase()método devuelve el valor de la cadena de llamada convertido a mayúsculas, de acuerdo con las asignaciones de casos específicas de la configuración regional.

// Algoritmo de ejemplo:

let cadena = 'MoziLLa';
const wordMay =(a) =>{
return a.toLocaleUpperCase();
}
console.log(wordMay(cadena));

